#pragma once

#include "glm/vec3.hpp"

using vec3 = glm::vec3;
namespace los
{

class Renderer
{
private:
    /* data */
public:
    Renderer(/* args */);
    ~Renderer();
    vec3 test();
};
    
    
} // namespace los
